#!/usr/bin/env uv run --script
# /// script
# requires-python = ">=3.11"
# dependencies = [
#     "click",
#     "tomlkit",
#     "psutil",
#     "semver",
# ]
# ///
# The above comment is managed by uv, do not edit it manually.
# It enables uv to manage the dependencies and the Python version.
# See PEP 723 for more information.
# https://peps.python.org/pep-0723/


from pathlib import Path
import sys
from typing import Final

# Make sure modules can be imported
SCRIPT = Path(__file__).resolve()
SCRIPT_DIR = SCRIPT.parent
MODULES_DIR: Final[Path] = SCRIPT_DIR / f'{SCRIPT.stem}_modules'
sys.path.insert(0, str(SCRIPT_DIR))
import devtool_modules # type: ignore # noqa: E402

if __name__ == "__main__":
    from devtool_modules import main  # type: ignore # noqa: E402
    main()

__all__ = ('devtool_modules', )
