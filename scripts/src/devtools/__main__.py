from devtools.main import main

if __name__ == "__main__":
    # This is the entry point for the command line interface.
    # It will be called when the module is run as a as a module,
    # e.g. python -m devtools.
    main()

